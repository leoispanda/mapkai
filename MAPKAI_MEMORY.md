# MapKAI Project Memory

This file is the portable working memory for continuing MapKAI development in a new Codex or ChatGPT window. It is not a full chat transcript. It records only the information needed to continue safely.

## 1. Project Path And Files

Current local repository:

```text
/Users/leoyang/Documents/Codex/2026-05-06/new-chat
```

Important files:

```text
index.html
script.js
styles.css
public/index.html
public/script.js
public/styles.css
server.js
assets/
public/assets/
```

Important sync rule:

```text
Root files are the main editable source.
public/ files must be kept in sync after frontend changes.
```

When editing:

```text
If index.html changes, sync public/index.html.
If script.js changes, sync public/script.js.
If styles.css changes, sync public/styles.css.
```

Local preview:

```text
PORT=3001 node server.js
http://127.0.0.1:3001/
```

Use the bundled Node path if needed:

```text
/Users/leoyang/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node server.js
```

## 2. Working Rules

Core rule:

```text
One update = One module = One clear commit
```

Development rules:

- Update by module.
- Change only the area requested.
- Do not regenerate the whole database.
- Do not rewrite the whole site unless explicitly requested.
- Do not do broad refactors without a strong reason.
- Do not automatically push unless the founder explicitly says to push.
- Before coding, read the current code structure first.
- Preserve existing user-approved behavior unless the new request clearly changes it.
- Keep root files and `public/` files synced.
- Run lightweight checks before reporting completion.

Preferred checks:

```text
node --check script.js
node --check public/script.js
git diff --check
```

Push rule:

```text
Commit and push only after the founder explicitly says "push".
```

## 3. Product Memory

MapKAI means:

```text
Map Knowledge with AI
```

Product concept:

MapKAI is a modular knowledge map and learning path system. It helps learners turn broad, scattered knowledge into clear maps, structured categories, guided learning paths, and eventually quiz-based validation.

Core metaphor:

```text
Unknown ocean -> snow mountain -> land -> green land
```

Meaning:

- Ocean = knowledge not explored yet.
- Snow mountain = first understanding.
- Land = knowledge understood well enough to explain.
- Green land = stronger mastery.

Main public modules:

```text
/           Home
/map        Knowledge map + blind quiz flow
/categories Knowledge classification system
/categories/:code Category detail pages
/learning   Learning path module
```

Module relationship:

- Map shows the big-picture learning landscape and quiz progress.
- Categories stores the official knowledge structure.
- Learning turns categories/fields into guided paths.
- Question banks should be internally tied to category codes, but normal users should not see those codes during quiz flow.

Founder mode:

Founder mode is for internal management only. It can show category codes, subject source, and maintenance details. Normal users should not see internal codes, subject IDs, database keys, or development notes.

## 4. Design Memory

Preferred visual direction:

- Clean
- Warm
- Premium
- Calm
- Product-like
- Slightly playful, but not childish
- Atlas / knowledge map feeling
- Educational, but not school-like
- Clear first-time visitor understanding

Preferred language style:

- Simple
- Direct
- Human
- Not overly abstract
- Not too internal-development oriented
- Metaphor is allowed only when it supports clarity

Avoid:

- Childish cartoon style
- Dark game UI
- Overly busy fantasy maps
- Too many labels on the map
- Internal wording such as "MVP", "do-not-touch boundaries", "semi-manual", "prove the template" in public-facing copy
- Showing category codes to normal users
- Making one category, especially 04, feel more important than all others on public pages

Map image direction:

The future map should ideally be designed as layered assets:

```text
Base ocean layer
Separate island/continent layers
Optional snow/land/green state overlays
Optional glow/progress overlay layers
Final preview composition
```

Reason:

Layered assets allow the site to light up specific land areas after users answer questions, without regenerating the full image every time.

## 5. Completed Work

Already completed:

- New modular MapKAI site structure.
- Home page with product positioning.
- Map / Categories / Learning navigation.
- 11 ISCED-F 2013 top-level categories loaded into the site.
- Category detail pages generated by code.
- Internal category codes hidden from normal users and visible in Founder mode.
- Contact section using `hello@mapkai.com`.
- Copyright notice.
- Updated MapKAI brand image.
- `/map` converted into a focused map + quiz page.
- 11-subject question bank architecture added.
- Each subject currently has 6 draft questions.
- Quiz runs as a blind flow: normal users do not see which subject a question belongs to.
- Founder mode shows the source subject/code for internal management.
- Unlock rule implemented:

```text
2 correct answers = Snow mountain
4 correct answers = Land
6 correct answers = Green land
```

Recent pushed commit:

```text
6cc19e4 Add blind map question bank flow
```

Verified:

- `node --check script.js`
- `node --check public/script.js`
- `git diff --check`
- Local `/map` quiz behavior:
  - answer appears
  - explanation appears after answering
  - Next question continues flow
  - 2 correct answers change state to Snow mountain
  - normal user does not see source subject

## 6. Current Todo

Recommended next work:

1. Improve `/map` visual design with a better layered map asset.
2. Replace or enrich the current draft question explanations with more natural, fun, analogy-based explanations.
3. Decide whether question bank data should be moved out of `script.js` into a dedicated data module later.
4. Build a clean internal question update format so each subject can be enriched without touching unrelated subjects.
5. Continue improving public copy only where it helps first-time users understand the product.

Draft or cautious areas:

- The question bank is still draft content.
- The current map drawing is functional but visually temporary.
- Learning paths are still broad structure, not full product logic.
- Categories are structurally loaded, but many detailed pages are not individually enriched.
- Avoid making `/map` show 11 category names again; the founder prefers focused quiz + map.

Do not redo:

- Do not re-add public 04 pilot emphasis.
- Do not show Chinese category titles on public cards unless explicitly requested.
- Do not show internal category numbers to normal users.
- Do not re-add phone number.
- Do not rebuild email backend unless explicitly requested.
- Do not expose PDC as a public site page.

## 7. Collaboration Preferences

When starting a new window:

1. Read this memory first.
2. Inspect the current repository files before editing.
3. Confirm the target module.
4. Make the smallest scoped change.
5. Sync `public/` files after frontend edits.
6. Run lightweight checks.
7. Summarize what changed and what was verified.
8. Do not push unless explicitly asked.

Preferred final report:

```text
Changed:
- ...

Verified:
- ...

Not pushed yet.
```

If pushed:

```text
Pushed:
- commit hash
- branch
```

## 8. New Window Startup Prompt

Copy this into a new Codex or ChatGPT window:

```text
We are continuing the MapKAI website project.

Please read and follow this project memory before making decisions:
MAPKAI_MEMORY.md

Current local repo:
/Users/leoyang/Documents/Codex/2026-05-06/new-chat

Core rules:
- One update = one module = one clear commit.
- Change only the requested module.
- Do not regenerate the whole database.
- Do not do broad refactors.
- Do not push unless I explicitly say "push".
- Root files and public/ files must stay synced.
- Normal users should not see internal category codes or question subject sources.
- Founder mode may show internal codes and source information.

Current focus:
Continue improving MapKAI carefully from the existing codebase. Before coding, inspect the current files and tell me what you are changing.
```

## 9. Update Log

### 2026-05-13 - Randomize map quiz flow

Status: Pushed

Changed:

- `/map` quiz now randomly draws from the full 11-subject question bank instead of starting at `01`.
- `Next question` tries to rotate to a different subject.
- Replaced repeated long MapKAI explanation text with: `Powered by MapKAI - map your knowledge with AI.`
- Root `script.js` and `public/script.js` were synced.

Verified:

- `node --check script.js`
- `node --check public/script.js`
- `git diff --check`
- Simulated repeated random question draws.

Commit:

- `c187de8 Randomize map quiz flow`

Push status:

- Pushed to `main`.

### 2026-05-13 - Map visual asset integration

Status: Included in next push

Changed:

- Added optimized layered map assets to `assets/map/` and `public/assets/map/`.
- Converted the generated `1672 x 941` image layers into `1100 x 619` web-sized PNG layers.
- Replaced temporary canvas-drawn islands with layered image rendering.
- Mapped subject mastery states to map layers:
  - `ocean` uses `unknown_ocean`
  - `snow` uses `partial_pale_snow`
  - `land` uses `golden_land`
  - `green` uses `full_green`
- Updated `/map` canvas dimensions to `1100 x 619`.
- Added a `Snow mountain` legend item.

Verified:

- `node --check script.js`
- `node --check public/script.js`
- `git diff --check`
- Root/public files synced.
- Confirmed all mapped layer files exist in both root and `public/`.

Notes:

- The current asset set has 9 visible islands and 2 reserved empty island layers.
- Future replacement of the 2 reserved island layers should only require asset updates, not a quiz rewrite.

### 2026-05-13 - Message board, quiz review, and visitor counter

Status: Prepared for push

Changed:

- Removed the public email input from the contact form.
- Kept `hello@mapkai.com` as the public contact address.
- Changed the contact form into a simple message board.
- Message board entries are saved in browser `localStorage`.
- Founder mode displays saved message board entries; normal mode hides the records.
- Correct quiz answers now immediately advance to another random question.
- Incorrect quiz answers show the explanation.
- Added `Previous` and `Next` review controls for answered questions.
- Added `/api/visit` Cloudflare Pages Function for real visitor counting with D1.
- Added footer visitor text that calls `/api/visit` and displays the returned visitor number.
- Added commented D1 binding instructions in `wrangler.toml`.
- Added `MAPKAI_MEMORY.md` as the unified project memory and update log.

Verified:

- `node --check script.js`
- `node --check public/script.js`
- `node --check functions/api/visit.js`
- Root/public files synced.
- `git diff --check`

Commit:

- This entry is part of the commit created for this update.

Push status:

- Push requested by founder; final pushed commit hash is reported in the Codex final response.

### 2026-05-13 - Refresh view counter cache key

Status: Prepared for push

Changed:

- Updated the `styles.css` and `script.js` cache version in `index.html`.
- Synced the same cache version change to `public/index.html`.
- This forces Cloudflare/browser caches to load the new view counter wording instead of the older visitor wording.

Verified:

- `git diff --check`
- Root/public `index.html` synced.

Commit:

- This entry is part of the cache refresh commit.

Push status:

- Push requested by founder; final pushed commit hash is reported in the Codex final response.

### 2026-05-13 - Split views and unique visitors

Status: Prepared for push

Changed:

- Changed the public footer counter from unique visitor wording to cumulative page views.
- Public mode now shows: `MapKAI has been viewed #X times.`
- Founder mode additionally shows the real unique visitor count.
- Updated `/api/visit` to store cumulative views in `site_stats` while keeping unique visitors in `visitors`.

Verified:

- `node --check script.js`
- `node --check public/script.js`
- `node --check functions/api/visit.js`
- `git diff --check`

Commit:

- This entry is part of the view counter update commit.

Push status:

- Push requested by founder; final pushed commit hash is reported in the Codex final response.

Notes:

- Real visitor counting requires a Cloudflare D1 database bound as `MAPKAI_DB`.
- Until D1 is configured in Cloudflare, the footer falls back to `Thanks for visiting MapKAI.`

### 2026-05-13 - Bind Cloudflare D1 visitor database

Status: Prepared for push

Changed:

- Bound the Cloudflare D1 database `mapkai` to the Pages project as `MAPKAI_DB`.
- Updated `wrangler.toml` with the D1 `database_id` so `/api/visit` can store real visitor IDs.

Verified:

- `git diff --check`

Commit:

- This entry is part of the D1 binding commit.

Push status:

- Push requested by founder; final pushed commit hash is reported in the Codex final response.

### 2026-05-13 - Hide founder entry and add bilingual support

Status: Prepared for push

Changed:

- Hidden public Founder mode entry while preserving Founder mode functionality.
- Added `/leoyangandxinli` as a private Founder mode route.
- Cleaned Home public copy and removed internal/development wording from public Home.
- Removed specific `04` / `0412` / Business / Finance homepage emphasis.
- Added English / Chinese language switch with localStorage persistence.
- Added Chinese public UI text across Home, Map, Categories, Category detail, Learning, Contact, and footer basics.
- Converted quiz data to bilingual `en` / `zh` blocks while keeping one shared question ID, subject, and difficulty per question.
- Added Chinese versions for all 66 existing quiz questions.
- Preserved existing quiz logic, Founder mode visibility rules, visitor counter, and Contact/localStorage logic.
- Reduced oversized Home typography slightly.
- Synchronized root and public files.

Verified:

- `node --check script.js`
- `node --check public/script.js`
- Bilingual quiz data validation for 66 questions.
- Root/public file sync checks.
- `git diff --check`

Commit:

- This entry is part of the bilingual Founder mode update commit.

Push status:

- Push requested by founder; final pushed commit hash is reported in the Codex final response.

### 2026-05-13 - Add quiz milestones, Grand Slam, and D1 contact messages

Status: Prepared for push

Changed:

- Added dynamic end-of-round completion state after all current questions are answered.
- Added Knowledge Grand Slam / 知识大满贯 achievement.
- Added exploration milestone titles based on answered question count.
- Preserved accuracy-based titles.
- Added bilingual milestone and Grand Slam modal copy.
- Avoided hard-coded question count to support future quiz expansion.
- Stopped automatic question cycling after full pool completion.
- Added manual new-round restart.
- Replaced local-only contact messages with Cloudflare D1-backed storage.
- Added public POST contact message API.
- Added Founder-only GET contact messages API.
- Added Founder mode message list.
- Preserved visitor counter logic.
- Preserved quiz random shuffle, language switch, and Founder mode route.
- Synchronized root and public files.

Verified:

- `node --check script.js`
- `node --check public/script.js`
- `node --check functions/api/contact-message.js`
- `node --check functions/api/contact-messages.js`
- `git diff --check`
- Root/public script and style sync checks.
- Local `/map` returned HTTP 200.
- Dynamic quiz validation confirmed full-pool no-repeat draw, completion stop, manual new-round restart, milestone thresholds, and Grand Slam modal.
- Contact API validation confirmed public POST success, empty message rejection, invalid email rejection, Founder-only GET protection, and Founder message retrieval.

Commit:

- This entry is part of the quiz milestones and D1 contact messages update commit.

Push status:

- Push requested by founder; final pushed commit hash is reported in the Codex final response.
